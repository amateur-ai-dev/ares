# Notes

## Workspace location

Lives in `docs/teaching/`, not the repo root — ARES root already carries
`MASTER_PLAN.md`, `resume.md`, `PAPER.md`. A second `MISSION.md` up there would
compete with them.

## Preferences observed (not yet stated by the user)

- **Caveman mode is on** for chat. Lessons themselves are written normally —
  they are documents to re-read later, not terminal output.
- Wants numbers, not reassurance. Has repeatedly caught overstated claims
  (the false 94.3% precision alarm, the void granite/qwen runs). Lessons should
  lead with what a number *cannot* prove.
- Deadline pressure is real. Keep lessons under ~10 minutes.

## Mission is inferred, not confirmed

`MISSION.md` was drafted from project context — paper + PPT + hackathon Q&A —
not from the user's own words. Confirm at the next opportunity and log a record
if it is wrong.

## Teaching angles still unused

- A drill lesson: I invent run blocks, user reads them back. Pure retrieval.
- The **hit-rate gap** (34% vs 81%) — the most attackable number, unaddressed.
- Why the **ceiling is 78%** and how it was measured. The claim a practitioner
  is most likely to challenge.
- Aporia as a *result*, not a failure — currently 0 in every run, which is
  itself worth being able to explain.
