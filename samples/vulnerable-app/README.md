# vulnerable-app — deliberately insecure sample

Planted defects for demonstrating the ARES code-review add-on. Every file here
contains a known weakness with a known CWE, so a demo can be checked against an
expected answer instead of against whatever the scanners happen to say.

**Do not run any of this.** It is written to be scanned. ARES never executes it.
